

## 运行程序 

```shell
$ cargo run --bin write_post # 增加文章
$ cargo run --bin show_posts # 展示已发布的文章
$ cargo run --bin publish_post n # 发布文章  使用时， n 替换为文章的id
$ cargo run --bin delete_post test  删除与test 相似的文章
```